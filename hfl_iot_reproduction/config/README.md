# Config

- `clients.yml`: mapeia IoT → mote_id (Intel Lab).
- `dataset.yml`: define janela temporal, features e alvo por IoT.
- `hyperparams.yml`: hiperparâmetros default.
- Variáveis de ambiente em `.env` sobrepõem estes valores.
